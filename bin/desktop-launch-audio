#!/usr/bin/env bash

# Launch Wiremix audio application in a new session with Ghostty
# Checking for other terminal emulators can be added if needed:wiremix

exec setsid uwsm app -- ghostty --title=Wiremix -e wiremix

# exec setsid uwsm app -- ghostty --title=Wiremix -e wiremix "$@"
