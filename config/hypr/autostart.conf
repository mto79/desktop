# Extra autostart processes
# exec-once = uwsm app -- my-service

# start nm-applet at login
exec-once = nm-applet

# The starting workspaces -- which screen each sits on, and what opens on it. Placed by
# the script rather than by `[workspace N]` exec rules here; see the note in the script.
exec-once = desktop-workspace-start
