# AUTO-GENERATED by desktop-workspace-pin -- edit the script, not this.
#
# Workspaces 1 to 5 are pinned to the screens they want -- the laptop panel, the widest
# landscape screen, a portrait screen -- falling back when the desk lacks one. The pin
# script runs at login and again whenever desktop-cmd-monitors-switch changes the
# layout; this copy ships without rules so a fresh install has something to source.
