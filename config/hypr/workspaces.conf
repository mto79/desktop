# AUTO-GENERATED by desktop-workspace-pin -- edit the script, not this.
#
# Workspace 3 is pinned to the widest landscape screen attached, so the terminal lands
# on the ultrawide at a desk and on the laptop panel without one. The pin script runs at
# login and again whenever desktop-cmd-monitors-switch changes the layout; this copy
# ships without a rule so a fresh install has something to source.
