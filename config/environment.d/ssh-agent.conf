# Hand SSH_AUTH_SOCK to the systemd user manager, which passes it to every
# service it starts -- including the Hyprland session, and so KeePassXC.
# Must match the socket path in systemd/user/ssh-agent.service.
SSH_AUTH_SOCK=${XDG_RUNTIME_DIR}/ssh-agent.socket
