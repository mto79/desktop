# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
windowrule = suppress_event maximize, match:class .*

# Opacity values used when transparency is toggled on (opaque by default)
windowrule = opaque true, match:class .*
windowrule = opacity 0.97 0.9, match:class .*

# Fix some dragging issues with XWayland
windowrule = no_focus true, match:class ^$, match:title ^$, match:xwayland true, match:float true, match:fullscreen false, match:pin false

# App-specific tweaks
source = ~/.local/share/desktop/default/hypr/apps.conf
